// import { Game } from "./game";

export function generateTooltip(game, type) {
    // takes in a tooltip type to be generated in the game, then creates and displays it
    // types: 'propertyHover'
}

export function generateInputWindow(game, type) {
    // takes in a window type to be generated in the game, then creates and displays it
    // types: ''
}

